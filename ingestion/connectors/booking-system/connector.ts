/**
 * Booking connector stub (Phase 3).
 * TODO: map PNR/connection changes into canonical events.
 */
export const bookingConnectorStub = {
  source: 'airline.ops.booking',
  emits: ['HighPassengerImpactDetected'],
};

