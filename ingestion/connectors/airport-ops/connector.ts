/**
 * Airport operations connector stub (Phase 3).
 * TODO: convert gate/stand updates into GateChanged events.
 */
export const airportOpsConnectorStub = {
  source: 'airline.ops.airport',
  emits: ['GateChanged'],
};

