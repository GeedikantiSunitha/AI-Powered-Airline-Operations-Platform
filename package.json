{
  "name": "@airline-ops/platform",
  "version": "0.1.0",
  "private": true,
  "description": "AI-Powered Airline Operations Platform — run scripts from backend/ or frontend/, or use root shortcuts below",
  "scripts": {
    "test": "npm run test --prefix backend && npm run test --prefix frontend",
    "test:backend": "npm run test --prefix backend",
    "test:frontend": "npm run test --prefix frontend",
    "test:unit": "npm run test:unit --prefix backend",
    "test:integration": "npm run test:integration --prefix backend",
    "test:gates": "npm run validate:phase11-gates --prefix backend",
    "typecheck": "npm run typecheck --prefix backend && npm run typecheck --prefix frontend"
  }
}
